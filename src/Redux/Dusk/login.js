import axios from 'axios';

//Constanes
const LoginState={
    array:[]
};

//types
const OPTENER_DATOS_TYPE="OPTENER_DATOS_TYPE";

//reducer

export default function loginreducer(state=LoginState,action){
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}

//Acciones
export const LoginAction = () => async (dispatch,getState) =>{
    try {
        
    } catch (error) {
        
    }
}
